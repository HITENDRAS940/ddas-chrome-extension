spring.application.name=DDAS

# Server Configuration
server.port=8080

# Database Configuration (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/ddas_db
spring.datasource.username=postgres
spring.datasource.password=12344321
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# AWS S3 Configuration
aws.s3.bucket-name=ddas-hash-files
aws.s3.region=ap-south-1
# ap-south-1 = Mumbai region (best for India)

# Email Configuration for OTP verification
# IMPORTANT: Replace these with your actual Gmail credentials
# 1. Use your Gmail address for spring.mail.username
# 2. Generate an App Password (not your regular Gmail password):
#    - Go to Google Account settings
#    - Security > 2-Step Verification > App passwords
#    - Generate a new app password for "Mail"
#    - Use that 16-character password below
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=testm2327@gmail.com
spring.mail.password=pwrvnloxzvsonnxt
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2

# OTP Configuration
app.otp.expiry-minutes=5
app.otp.max-attempts=3
# Note: Configure AWS credentials via AWS CLI or environment variables
# AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY

# File Upload Configuration
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# CORS Configuration (for Chrome Extension)
cors.allowed.origins=chrome-extension://*

# JWT Configuration
app.jwt.secret=juasdnfdsf%%%fjhsdfjf01293123vnBdh7g8gv8v783y4983b4jb4jbj3j4jk3h4j3hjh3j4hj3h4j3h4j3
app.jwt.expiration-ms=86400000
# 86400000 ms = 24 hours
